from sys import stdin
def isPalindrome(string):
    # Your code goes here
    rev = string[::-1]
    if rev == string:
        return True
    else:
        return False
string = stdin.readline().strip();
ans = isPalindrome(string)

if ans:
    print('true')
else:
    print('false')
